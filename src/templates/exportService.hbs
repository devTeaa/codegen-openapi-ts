{{>header}}

{{#if imports}}
{{#each imports}}
import type { {{{this}}} } from '../models/{{{this}}}';
{{/each}}
{{/if}}

{{#each operations}}
{{#hasLength parameters}}
export type {{capitalizeFirstLetter name}}Request = {
  {{>parametersType}}
}

{{/hasLength}}
{{/each}}
